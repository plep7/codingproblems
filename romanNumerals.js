var RomanNumeral = {
  toRoman(number) {
    console.log(number)
  },
  fromRoman(numeral) {
    console.log(numeral)
    // initialize a result variable to 0
    // iterate through the string
    // if the next value is smaller, subtract from total
    // else add it to the total
  }
}


var conversion = {
  I: 1,
  V: 5,
  X: 10,
  L: 50,
  C: 100,
  M: 1000
}

RomanNumeral.toRoman(1000)
RomanNumeral.fromRoman('M')
